# Tupla vacía
vacia = ()

# Tupla con elementos
punto = (10, 20)

# IMPORTANTE: Tupla de un solo elemento (requiere una coma)
uno = (5,)
# Sin la coma, Python lo interpreta como un número entre paréntesis: uno = (5) -> int

# Crear tupla desde otra colección
lista_num = [1, 2, 3]
tupla_num = tuple(lista_num)