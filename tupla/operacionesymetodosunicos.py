valores = (1, 2, 3, 2, 4, 2)

# count(): Cuenta cuántas veces aparece un valor
print(valores.count(2)) # 3

# index(): Devuelve la primera posición de un valor
print(valores.index(4)) # 4

# Concatenación (Crea una tupla nueva)
union = (1, 2) + (3, 4) # (1, 2, 3, 4)

# Multiplicación
repetida = ("A",) * 3 # ("A", "A", "A")