# Set vacío (¡Cuidado! {} crea un diccionario, no un set)
mi_set = set()

# Set con elementos iniciales
frutas = {"manzana", "pera", "uva"}

# Crear un set desde otra colección (elimina duplicados automáticamente)
numeros = set([1, 2, 2, 3, 4, 4, 4])
# Resultado: {1, 2, 3, 4}